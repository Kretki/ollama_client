Additionally generate complete, self-contained test implementations for all recommended test cases. Choose and justify one testing framework per language. Produce full test source files (e.g., `test_filename.cpp`, `test_module.py`) including all required headers, fixtures, assertions, and OpenGL off-screen verification helpers where graphics code is present. Supply build or run commands (CMake target, Makefile rule, pytest invocation) and any minimal mock or stub code. Present each generated test file under its own Markdown heading with filename and fenced code block. Ensure every test file is compilable or importable with only the chosen framework and the project’s existing dependencies.